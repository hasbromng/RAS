# RAS Monitoring Agent - Core Dependencies

# System metrics collection
psutil>=5.9.0

# HTTP requests
requests>=2.28.0

# Task scheduling (optional, can be used for alternative scheduling)
schedule>=1.2.0

# Windows service support (Windows only)
pywin32>=305; sys_platform == 'win32'

# WMI support for Windows
wmi>=1.5.1; sys_platform == 'win32'
pypiwin32>=223; sys_platform == 'win32'
